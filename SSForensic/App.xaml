<Application x:Class="SSForensic.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:conv="clr-namespace:SSForensic.Views"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Dark" PrimaryColor="Grey" SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <conv:InvertBooleanConverter x:Key="InvertBooleanConverter"/>
            <conv:FlagToBrushConverter x:Key="FlagToBrush"/>
            <conv:TrustToBrushConverter x:Key="TrustToBrush"/>
            <conv:BoolToVisConverter x:Key="BoolToVis"/>
            <conv:HasRunToScrimOpacityConverter x:Key="HasRunToScrim"/>

            <!-- Trust dot colors -->
            <SolidColorBrush x:Key="ColorReplaceWithJava" Color="#E06C75"/>
            <SolidColorBrush x:Key="ColorOriginalCheat"   Color="#61AFEF"/>
            <SolidColorBrush x:Key="ColorOriginalLegit"   Color="#7EC07E"/>
            <SolidColorBrush x:Key="ColorNoSignature"     Color="#E06C75"/>
            <SolidColorBrush x:Key="ColorExtSpoofed"      Color="#C678DD"/>

            <!-- Forensic-tool palette (PrefetchView++ / BAM parser style) -->
            <SolidColorBrush x:Key="BgBase"        Color="#2A2D31"/>
            <SolidColorBrush x:Key="BgHeader"      Color="#33363B"/>
            <SolidColorBrush x:Key="BgPanel"       Color="#2F3236"/>
            <SolidColorBrush x:Key="BgPanelAlt"    Color="#2A2D31"/>
            <SolidColorBrush x:Key="BgRow"         Color="#2A2D31"/>
            <SolidColorBrush x:Key="BgRowAlt"      Color="#2F3236"/>
            <SolidColorBrush x:Key="BgSelected"    Color="#3D4148"/>
            <SolidColorBrush x:Key="Border1"       Color="#3D4147"/>
            <SolidColorBrush x:Key="Border2"       Color="#4A4E55"/>
            <SolidColorBrush x:Key="TextPrimary"   Color="#D7D7D7"/>
            <SolidColorBrush x:Key="TextSecondary" Color="#A0A0A0"/>
            <SolidColorBrush x:Key="TextMuted"     Color="#7A7A7A"/>
            <SolidColorBrush x:Key="Accent"        Color="#5FA8D3"/>
            <SolidColorBrush x:Key="AccentDim"     Color="#3A6E8E"/>
            <SolidColorBrush x:Key="SignedGreen"   Color="#7EC07E"/>
            <SolidColorBrush x:Key="UnsignedRed"   Color="#E06C75"/>

            <!-- Named text styles used via x:Key (never implicit) -->
            <Style x:Key="TitleText" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="Foreground" Value="#E6E6E6"/>
            </Style>
            <Style x:Key="LabelText" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Foreground" Value="#A0A0A0"/>
            </Style>
            <Style x:Key="MonoText" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Consolas, Courier New, monospace"/>
                <Setter Property="FontSize" Value="11.5"/>
                <Setter Property="Foreground" Value="#D7D7D7"/>
            </Style>

            <!-- Named button styles (referenced explicitly, safe) -->
            <Style x:Key="FlatButton" TargetType="Button">
                <Setter Property="Background" Value="#3D4147"/>
                <Setter Property="Foreground" Value="#D7D7D7"/>
                <Setter Property="BorderBrush" Value="#3D4147"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="2">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#474B52"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#34383E"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="b" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource FlatButton}"/>
            <Style x:Key="FilterChip" TargetType="Button" BasedOn="{StaticResource FlatButton}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
